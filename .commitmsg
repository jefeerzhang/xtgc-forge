refactor(文档解析): 三套章节切分合并为 md_doc 深 module（v0.3.21）

架构打磨候选 #2（/improve-codebase-architecture 报告，grilling 六项决策落定）。

为什么改:
上一轮七项低危修复全部落在章节切分边界——不是巧合，是三套各自实现
的切分（六段按标题候选切 / 附录 A→B 按正则切 / 正文按整合附录正则切）
共用同一个脆弱概念却互不同步。测试直测下划线函数（_extract_section、
_strip_md_structure）是另一个信号：seam 实际存在，只是没被承认为
interface。

技术方案（六项决策）:
- 新建 scripts/md_doc.py（标题树模型）：解析一次，分节边界统一
  「同级或更高级标题」，深层同名小节不截断
- 三个切分调用点变为对树的查询：section_text（六段正文）/
  appendix_range（附录范围）/ body_before（正文）；
  strip_structure（散文提取）一并收入
- 匹配语义：标题优先 + 内部回退文本标记（沿用历史正则语义），
  不给无 # 前缀附录标题的既有报告形态制造新误报
- check_step.py 删除 _extract_section / _strip_md_structure
  （1075→992 行），消灭从未真正起作用的 next_headings 参数
- 直测下划线的用例迁移到 md_doc interface；64 个行为级用例
  原样保留作回归锚
- 走升版惯例：v0.3.20 → v0.3.21 五处同步 + CHANGELOG 叙事

domain 沉淀:
- 新建仓库根 CONTEXT.md，登记 标题树 / md_doc / 切分查询 / 散文提取

验证:
- pytest 80 passed（64 行为锚 + 16 新 interface 用例）
- python scripts/check_step.py --help 直跑路径通过
- marketplace.json 双 version 字段均为 0.3.21
