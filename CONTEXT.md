# CONTEXT · xtgc-forge 领域词汇

> 本文件是本项目 domain language 的单一来源。engineering skills（grill-with-docs、
> domain-modeling、improve-codebase-architecture）读写本文件；引入新概念或打磨
> 模糊术语时同步更新。架构决策记录在 `docs/adr/`（尚未有，首个候选被拒绝时创建）。

## 核心概念

- **主报告（主报告模板产物）**：Step 6 交付的整合报告，含六段论述正文 + 整合附录 A–F。
- **闸门（gate）**：`check_step.py` 中的校验规则族；只消除误报，不放松拦截语义。
- **整合附录**：主报告尾部的技术对照区（附录 A 文献矩阵、C 编号对照、F 决策表等），
  可读性闸门豁免区。
- **正文**：主报告开头到整合附录标题之前的部分，受黑话禁令与断句闸门约束。

## 模板契约（v0.3.22 引入）

- **templates**：`scripts/templates.py`，「产物文件应该长什么样」的深 module。
  14 个 init 模板字面量与占位符拦截规则同址；init 从它写盘，闸门从它
  取拦截模式，不存在第二份手写副本。
- **派生拦截**：模板文本中出现的每个 `<...>` 占位 token 自动成为闸门拦截对象；
  HTML 注释行是编辑提示，不参与派生。不变式：模板占位必被拦截。
- **FILL_TOKENS**：init 替换的参数化 token 集合（日期/主题/分支/语言），
  与替换实现双向断言锚定。
- **通用闸规则**：针对用户自造占位符（TODO/待填/≥4 汉字尖括号式）的拦截，
  与模板契约正交，留在 check_step.py。

## 文档解析（v0.3.21 引入）

- **md_doc**：`scripts/md_doc.py`，Markdown 结构理解的深 module。唯一职责：
  把报告文本变成可查询的结构。闸门只向它提问，不自行切分。
- **标题树（heading tree）**：md_doc 对一份文档的解析结果——按出现顺序的标题
  列表，每个标题含级别、文本、起止行。分节边界统一语义：**同级或更高级**的标题
  才算边界，深层同名小节不截断。fenced code 与 4 空格缩进代码块内的 `#` 行
  不进入标题树（避免把 Python/Stata 注释当「整合附录」分界）。
- **切分查询**：调用方对标题树的三种提问——取某节正文（六段）、取附录范围
  （附录 A 在同级/更高级标题或下一附录字母处截断，取较早者；文本标记回退时
  额外在 h1/h2 处停）、取正文（整合附录标题之前）。
- **散文提取（strip）**：从一段文本剥掉 markdown 结构（标题/表格/代码块/公式块/
  分隔线/列表与引用标记），只留可断句的真散文。属于 md_doc，不属于闸门。

### md_doc 架构设计的六项决策记录 (v0.3.21)

1. **单次解析构建标题树**：避免多次遍历与正则扫描，单次将 Markdown 解析为统一层级标题树，所有切分操作均作为对树的只读查询。
2. **同级或更高级截断原则**：分节终点严格判定为「同级或更高级别的后续标题」，彻底解决深层同名小节导致外层章节被提前截断的 bug。
3. **三种正交查询接口解耦**：将原有混杂逻辑拆分为三个独立接口——`section_text`（提取六段式核心论述）、`appendix_range`（附录边界截取）、`body_before`（附录前纯正文提取）。
4. **混合匹配语义兼容（标题优先 + 文本标记回退）**：优先匹配 Markdown 标准标题（`#`），对历史或无 `#` 前缀的附录标记保留文本回退匹配，杜绝为存量旧报告引入新误报。
5. **死参数清理与单一职责**：从 `check_step.py` 完全剔除私有函数 `_extract_section` 与 `_strip_md_structure`，并彻底废弃未生效的 `next_headings` 参数。
6. **测试断言从私有函数转向公共接口**：测试用例直接面向 `md_doc` 暴露的标准接口断言，将原本直接测试内部私有函数的脆弱用例固化为稳定的 interface 契约测试。

## 供应链

- **vendored skill**：`vendor/<skill>/` 下的上游学术技能副本，以 `VERSION.md`
  登记上游元数据；`scripts/vendor_sync.sh` 负责漂移探测与 3-way merge 同步（list/check/fetch/diff/apply）。
  仓库根只认 `XTGC_ROOT`（且其下有 `vendor/`）或脚本上一级，不用 `pwd`。
  `apply` 的 merge-base 是 `vendored_commit`（须为本仓 git 对象），不是 `HEAD`；
  比不过则冲突不覆盖。probe 从 `vendor/` 派生名单，本仓同步点 SHA 不与上游 HEAD 比漂移。
  `init_project` 与闸门同样识别 `process/`：已有过程文件时不得在根目录写空模板抢读。
