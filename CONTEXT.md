# CONTEXT · xtgc-forge 领域词汇

> 本文件是本项目 domain language 的单一来源。engineering skills（grill-with-docs、
> domain-modeling、improve-codebase-architecture）读写本文件；引入新概念或打磨
> 模糊术语时同步更新。架构决策记录在 `docs/adr/`（尚未有，首个候选被拒绝时创建）。

## 核心概念

- **主报告（主报告模板产物）**：Step 6 交付的整合报告，含六段论述正文 + 整合附录 A–F。
- **闸门（gate）**：`check_step.py` 中的校验规则族；只消除误报，不放松拦截语义。
- **整合附录**：主报告尾部的技术对照区（附录 A 文献矩阵、C 编号对照、F 决策表等），
  可读性闸门豁免区。
- **正文**：主报告开头到整合附录标题之前的部分，受黑话禁令与断句闸门约束。

## 文档解析（v0.3.21 引入）

- **md_doc**：`scripts/md_doc.py`，Markdown 结构理解的深 module。唯一职责：
  把报告文本变成可查询的结构。闸门只向它提问，不自行切分。
- **标题树（heading tree）**：md_doc 对一份文档的解析结果——按出现顺序的标题
  列表，每个标题含级别、文本、起止行。分节边界统一语义：**同级或更高级**的标题
  才算边界，深层同名小节不截断。
- **切分查询**：调用方对标题树的三种提问——取某节正文（六段）、取附录范围
  （附录 A→B，标题优先、文本标记回退）、取正文（整合附录标题之前）。
- **散文提取（strip）**：从一段文本剥掉 markdown 结构（标题/表格/代码块/公式块/
  分隔线/列表与引用标记），只留可断句的真散文。属于 md_doc，不属于闸门。

## 供应链

- **vendored skill**：`vendor/<skill>/` 下的上游学术技能副本，以 `VERSION.md`
  登记上游元数据；`vendor_sync.sh` 负责漂移探测与 3-way merge 同步。
